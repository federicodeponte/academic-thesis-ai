\documentclass[sigconf]{acmart}

% Remove ACM copyright/conference info for thesis use
\setcopyright{none}
\settopmatter{printacmref=false}

\usepackage{hyperref}

% Hyperlink styling
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=blue,
    citecolor=blue,
}

% Title and author information
\title{$title$}

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

\author{$author$}
$if(institution)$
\affiliation{
  \institution{$institution$}
  $if(department)$
  \department{$department$}
  $endif$
  $if(city)$
  \city{$city$}
  $endif$
  $if(country)$
  \country{$country$}
  $endif$
}
$endif$

$if(email)$
\email{$email$}
$endif$

\begin{document}

\maketitle

% Abstract
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

% Keywords
$if(keywords)$
\keywords{$for(keywords)$$keywords$$sep$, $endfor$}
$endif$

% Main content
$body$

% References
$if(bibliography)$
\bibliographystyle{ACM-Reference-Format}
\bibliography{$bibliography$}
$endif$

\end{document}
